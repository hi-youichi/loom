[package]
name = "loom-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

description = "Core utilities and algorithms shared across Loom crates"

[dependencies]
regex = "1"
