[package]
name = "loom-pty-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

description = "PTY wire-protocol helpers mirroring the opencode `pty-protocol.ts` contract"

[dependencies]
