{
  "name": "web-audit",
  "private": true,
  "type": "module",
  "description": "Playwright-based first-paint audit: collects page errors, console messages, failed requests and HTTP error responses.",
  "scripts": {
    "setup": "playwright install chromium",
    "audit": "node audit.mjs"
  },
  "dependencies": {
    "playwright": "^1.62.0"
  }
}
