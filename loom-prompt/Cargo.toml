[package]
name = "loom-prompt"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "System prompt assembly, environment context, and prompt loading"

[dependencies]
which = "4.0"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "1.0"
