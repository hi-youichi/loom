//! RecordingCollector (SQLite-backed persistence) — Phase 2 implements.
//!
//! Phase 1 only defines this module as a placeholder so the `mod` declaration
//! in `stats/mod.rs` resolves. The actual `RecordingCollector` lives in
//! `stats/record.rs` once Phase 2 wires `rusqlite` into `agent-core`'s
//! `Cargo.toml`.

// Intentionally empty in Phase 1.