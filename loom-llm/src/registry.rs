//! Registry module for managing LLM models.

/// Model registry for storing and retrieving model information.
pub struct ModelRegistry {
    // Placeholder - full implementation in loom crate
}

impl ModelRegistry {
    /// Creates a new empty registry.
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for ModelRegistry {
    fn default() -> Self {
        Self::new()
    }
}